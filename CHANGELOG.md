# 更新摘要 / Changelog

## v1.1.18

**修复**
- 根治排版错乱与光标错位：PTY 列数此前取自首次（未稳定的窄）布局，握手完成后新增**最终尺寸校准**——以 View 最新列数重设 PTY 与缓冲区，banner 按真实屏宽折行，光标回到提示符 # 后
- 修复输入法无法调出：点按终端即聚焦并拉起键盘；回看历史时点按回到底部

## v1.1.17

**修复**
- 终端配色跟随日夜主题：日间模式白底黑字，不再白屏看不清文字
- 光标跟随滚动：可视窗口自动平移，提示符（root@…#）始终可见，不再"藏"在屏幕中部或键盘下方
- 触控上滑回看历史输出，下滑回到底部

**体验增强**
- 长按进入选择模式，拖选文本后点「复制选中」复制到剪贴板
- 终端字号调节（A−/A+）结果持久化，下次连接保持

## v1.1.16

**修复**
- SSH 输入即断开的最终定位：用户输入在 **Android 主线程**直接写入 SSH 通道，sshj 的通道写内部锁/窗口等待与传输读线程竞争（该问题在桌面 JVM 无法复现，Android 线程调度下触发服务器主动断开）——已用与你服务器完全相同的库与参数在真实主机上完成端到端复现与验证，现将写入移至 IO 线程
- 复现验证结论：连接、认证、输入回显、空闲 35 秒后再输入、长会话全部正常

## v1.1.16

**修复**
- 彻底移除 SSH keepalive：sshj 保活线程与交互写入并发会导致服务器以 "Disconnected" 主动断开（实测约 30 秒后）；交互会话依赖 TCP 存活，socket 读超时保持 0 不再空闲掐断
- 键盘弹出不再修改 PTY 行数：此前键盘收起/弹出会把服务器屏幕模型与本地显示弄错位，光标（root@sg:~#）被"藏"在键盘下方；现在行数变化仅调整本地渲染，光标始终可见
- 列数变化（旋转屏幕）仍同步服务器

## v1.1.15

**修复**
- 修复键盘弹出（触发尺寸调整）与服务器回显并发操作终端缓冲区导致的崩溃性断开：缓冲区读写加互斥锁，喂入瞬时异常不再终止会话
- 终端尺寸上报增加 200ms 防抖：连接改用布局稳定后的最终尺寸发起（修正 banner 按过窄宽度换行的错乱），键盘动画期间不反复 resize
- 输入发送失败时明确显示断开原因，不再静默丢键

## v1.1.14

**修复**
- 修复 v1.1.13 备份导出仍失败的问题：导出的传统通道把**文件名误传为 MIME 类型**（`typ=xxx.json` 导致无应用可处理）；现 MIME 固定为 application/json，文件名仅作为建议标题传入
- androidx CreateDocument 在 ColorOS 15 上的 requestCode 溢出异常仍由传统通道兜底（该通道实测可用）

## v1.1.13

**修复**
- 修复 SSH 握手阶段未设置 socket 读超时导致连接异常的问题：握手期间限制等待 12 秒，连接建立后清除 socket 超时，终端长时间空闲不会断线

## v1.1.13

**修复**
- 备份导出/恢复弹出的系统文件选择器与此前私钥选择同源的问题（ColorOS 15 对 androidx 启动路径同步抛异常导致 App 退出前台）：接入与私钥选择相同的**多通道回退机制**（CreateDocument/OpenDocument/GetContent/Chooser/传统 startActivityForResult），任一通道可用即正常工作
- 全部通道失败时弹窗展示各通道异常详情并可一键复制，同时写入诊断日志

## v1.1.12

**修复**
- SSH 会话约 12 秒空闲后必断开：此前给 SSH 客户端设了 socket 读超时，交互式 shell 空闲即被掐断；改为仅限制连接建立耗时 + 30 秒 keepalive 保活探活
- 终端列数错乱（文本在约 27 列就换行）：尺寸只在首次布局取一次，改为**动态上报**——键盘弹出/旋转/布局稳定后自动调整 PTY 与缓冲区，服务器端程序同步重排
- 修复连接建立期间多次布局导致 PTY 仍采用首次临时列数的问题：连接完成后以最新可视尺寸初始化终端，并向服务器补发窗口调整
- 键盘弹出遮挡终端下半部：终端页接入 imePadding 避让
- 断开提示不再居中遮挡终端内容，改为顶部横幅，断开前的输出仍可查看

## v1.1.11

**修复**
- 修复首次 SSH 连接报「SSH 错误: HOST_KEY_UNKNOWN」且无法继续的问题：TOFU 主机密钥确认异常此前被包装成通用错误，导致“确认主机指纹”对话框永远弹不出来；现在首次连接会正常弹出指纹确认，点「信任并连接」后即可建立会话（指纹同时保存，后续防中间人）
- 编辑 SSH 主机时明确显示凭据保存状态：私钥显示「已保存 ✓（格式 · 长度）」，密码显示「已保存 ✓」，不再无法确认是否保存成功

## v1.1.10

**优化**
- 首页大卡片移除 CPU 走势图（与半圆仪表功能重复），卡片更紧凑
- 半圆仪表百分比改为**保留一位小数**：空闲服务器 0.3% 这类低占用不再被取整成 0%

## v1.1.9

**修复**
- 探针修复内存总量始终为 0 的问题（Linux 采集器漏填 MemTotal，导致 App 显示「553 MB / 0 B」）：升级探针后总量正常显示
- App 防御显示：对接旧版探针（总量缺失）时只显示已用量，不再出现「/ 0 B」；交换分区同理

## v1.1.8

**优化**
- 首页大卡片 CPU / 内存 / 磁盘更换为**半圆形仪表**：弧形进度在上、百分比居中、名称与占用详情在下方（≥70% 转橙、≥90% 转红）
- 流量行过长被省略的问题：拆为两行完整显示（今日收发 / 本月收发与总量）
- CPU 走势图纵轴改为**动态量程**：空闲服务器低占用波动也清晰可见，不再是一条贴底死线

## v1.1.7

**修复**
- 首页大卡片 CPU / 内存 / 磁盘三项由三列挤排改为**每行一项**，进度条通栏、标签与数值左右对齐，长数值不再换行错位
- 主机详情页概览的 CPU / 内存仪表同步改为通栏排版

## v1.1.6

**整体优化与安全加固（代码审计轮）**

安全：
- 远程 WebView 禁用混合内容（https 页面不再加载 http 子资源）
- 远程页外链跳转仅放行 http/https，防止远程页面借 `intent://` 触发任意应用组件
- 应用内更新的 APK 下载增加 300MB 大小上限，异常通道不再可能写满磁盘

性能：
- SSH 终端输出**批量合并**处理：`cat` 大文件等高吞吐场景下解析与重绘次数大幅减少，滚动更流畅
- 终端绘制改用 StringBuilder 复用（原每帧每行字符串拼接为 O(n²)）
- 探针轮询的 OkHttpClient **按信任配置全局复用**，不再每轮新建连接池与线程
- 终端页键盘拉起仅触发一次（原每次输出重绘都会调用系统接口）

代码质量：
- 迁移全部弃用 API 至新版本：scaledDensity→TypedValue、onBackPressed→ predictive back 回调、RequestBody.create→扩展函数、Room destructiveMigration 新签名、menuAnchor 新重载
- 全量编译零警告、零未使用代码

## v1.1.5

**修复**
- 修复 Android 15（强制边到边）上「远程」WebView 页与添加/编辑链接页的顶栏与系统状态栏重叠问题：两页内容现在正确避让系统栏、刘海与键盘（键盘弹出时自动收起底边）

## v1.1.4

**新功能（探针 + App 需同步升级）**
- 探针新增**持久化流量统计**：今日 / 本月 / 总量三种口径（`traffic.json` 每分钟原子落盘、退出时落盘），服务器重启后自动识别计数回绕继续累计，日/月自动翻转清零
- 速率显示保持原自适应单位不变；详情页「网络」区新增**流量统计块**（今日/本月/总计，各自下行↑上行，固定 MB 显示）
- 首页大卡片新增流量行：今日收发速率与本月/总用量一览
- 统计过滤 lo/veth/docker 等虚拟网卡，避免容器流量重复计数
- 旧版探针（无流量字段）App 自动隐藏统计块，兼容不报错

## v1.1.3

**修复**
- 修复切换到「远程」tab 时先闪一帧“还没有远程电脑”空态、再刷新出列表的体验问题：页面数据改为构造时同步预载，首帧即显示已保存链接
- 「管理」tab 的探针主机与 SSH 列表同样做了首帧预载，消除同类闪烁

## v1.1.2

**优化**
- 「安全」开关指示器移至与标题同一行，版式更协调
- 「检测更新」按钮移至标题位并移除重复标题，卡片更简洁
- **备份与恢复纳入远程链接**：导出/恢复均包含「远程」页添加的 ZCode 链接（同 id 覆盖、其余保留），恢复完成提示分别列出三类数量

## v1.1.1

**修复/优化**
- 开启「生物识别锁」前需**先通过指纹/PIN 验证**才生效：设备未录入凭据或验证未通过/取消时保持关闭，防止误开或被他人开启
- 设置页各分区卡片**统一宽度**；「状态刷新间隔」选项自动换行，60秒 选项不再被裁切

## v1.1.0

**新功能**
- 集成 **Zrmt（ZCode 远程控制链接管理器）**：新增底部导航「**远程**」tab
  - 卡片式管理多台电脑的 ZCode 远程控制链接（粘贴链接自动解析电脑名称与版本号，需含 sid/hash 参数）
  - 点击卡片在 App 内 WebView 打开操作界面：支持旋转、桌面/手机版 UA 切换、错误重试、网页内附件/图片上传（系统文件选择器接入）、摄像头/麦克风网页授权
  - 支持从其他应用「分享链接」到云枢快速添加
- 首页改版：底部新增 **管理 / 远程 / 设置** 三 tab 导航栏；右下角悬浮「＋」添加菜单移至**右上角**

**优化**
- 整体代码清理：移除非必要的调试/测试辅助代码与死代码（未使用函数、Token 随机生成按钮等）

## v1.0.6

**修复（国产 ROM 专项二轮）**
- ColorOS 15 上 5 条 SAF 通道全部同步失败，说明问题出在公共启动路径：新增**第 6 通道「传统 startActivityForResult」**，完全绕开 androidx ActivityResultRegistry 与 ActivityOptions，直接走框架最老路径
- 全通道失败时改为**弹窗展示每条通道的完整异常**并支持一键复制（此前仅 toast），同时写入诊断日志，便于精确定位 ROM 拦截点
- 粘贴私钥输入始终可用（效果与选文件完全一致）

**提示**
- 若第 6 通道也失败且详情指向权限/管控，请在 ColorOS「设置 → 应用 → 云枢」中检查权限与电池策略；或直接使用粘贴私钥

## v1.0.5

**修复（国产 ROM 专项）**
- 定位到选私钥闪退/被切出前台的根本原因：**ColorOS 15 等系统对 ACTION_GET_CONTENT 通配 MIME 会同步抛出 IllegalArgumentException**（感谢用户提供的诊断信息确认）
- 文件选择改为 **5 通道自动回退**，任一通道可用即可打开：OpenDocument → GetContent(二进制) → GetContent(文本) → GetContent(通配) → 系统选择器 Chooser 中转
- 所有通道失败时明确提示改用「粘贴私钥内容」，并将每条通道的详细异常**写入诊断日志**（设置 → 关于 → 复制诊断日志），便于继续定位
- 粘贴私钥输入（自动识别 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK）保持可用，作为终极保底

## v1.0.4

**修复**
- 彻底解决「添加 SSH 主机选择本地私钥时 App 被切出前台」：
  - 新建表单**草稿持久化**（Keystore 加密落盘），即使部分 ROM 在系统文件选择器置顶期间查杀后台进程，返回应用后表单与所选私钥**原样恢复**，不再前功尽弃
  - 私钥支持**直接粘贴输入**（自动识别 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK 格式），完全不依赖系统文件选择器
  - 文件选择器启动失败（精简 ROM 无组件等）时给出明确提示并引导粘贴输入，不再静默退出

**说明**
- 文件选择基于系统 SAF 机制，**无需任何存储权限**（应用仅申请网络/生物识别/安装更新三项权限）
- 若你的 ROM 会在选择器打开时查杀应用，建议在系统设置中将云枢的电池策略设为「无限制/不限制后台」

**新增**
- 设置 → 关于：**复制诊断日志**（设备信息 + 最近一次崩溃堆栈）到剪贴板，便于反馈问题

## v1.0.3

**修复**
- 修复开启指纹锁后，添加 SSH 主机选择本地私钥文件时弹出认证、表现为被切出前台的问题（此前每次回到前台都会重新触发认证）

**变更**
- 生物识别认证策略调整为**仅在应用启动时验证一次**：切换后台、使用系统文件选择器、修改配置等场景均不再触发认证；锁定页支持「重试解锁」

## v1.0.2

**变更**
- 应用更名为「**云枢**」（英文 **Remoto**），应用包名变更为 `com.sundys.remoto`（与旧版本并存安装，需卸载旧版）

**体验优化**
- 首页新建按钮改为**圆形「＋」**悬浮按钮，SSH 列表**上滑自动隐藏、下滑恢复显示**
- 修复：开启生物识别锁后，添加 SSH 主机选择本地私钥时会被误锁切出前台的问题（系统文件选择器期间不再触发应用锁）

**项目**
- 探针 Release 资产改为固定名称（serverprobe-linux-amd64/arm64.tar.gz），README 提供自动识别架构的**探针一键安装命令**（含国内加速前缀说明）

## v1.0.1

**新功能**
- 设置页新增「检测更新」：一键检测 GitHub 最新 Release，支持**多通道检测与下载**（直连 + gh-proxy / ghfast / moeyy / ghproxy 等加速代理自动回退），解决部分网络无法访问 GitHub 的问题
- 检测到新版本直接弹出更新窗口，展示更新摘要与安装包大小，一键「下载并安装」
- 下载过程实时显示**进度条与已下载/总大小/百分比**，下载完成后自动唤起系统安装器
- 新增「关于」菜单：应用介绍，页面底部展示开源主页入口（GitHub 图标 + 「Github主页」超链接直达仓库）

**优化**
- 正式包按 ABI 拆分（armv7 / arm64）并使用固定签名，更新包可直接覆盖安装

## v1.0.0

🎉 首个公开版本：服务探针管理系统（Android 管理端 + Go 服务器探针）

**Android 管理端**
- 首页大卡片左右滑动切换主机，实时显示 CPU / 内存 / 磁盘 / 网速 / 负载 / 运行时长与 CPU 走势图
- 主机详情页：每核 CPU、内存与交换、多磁盘、网卡速率、systemd 服务列表（支持启动/停止/重启），底部 SSH 快速连接菜单
- SSH 终端：密码 + 私钥认证，私钥兼容 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK v2/v3；自研 VT100 256 色终端，控制键条、粘贴、字号调节；首次连接指纹确认（TOFU 防中间人）
- 添加探针向导：内置 SSH 设置项（绑定已有身份或新建）、连接测试、证书指纹获取与锁定
- 设置：日间/夜间/跟随系统显示模式、刷新间隔、生物识别锁、口令加密备份与恢复（PBKDF2 + AES-256-GCM）
- 凭据全部经 Android Keystore AES-256-GCM 加密存储；全局禁止明文流量

**Go 服务器探针**（本仓库 `probe/` 目录，linux amd64 / arm64）
- 零第三方依赖单二进制，systemd 常驻，一键安装脚本
- CPU/内存/磁盘/网络/负载/进程/服务状态采集 API
- Bearer Token 常量时间校验 + 每 IP 限流 + TLS 自签证书 + 服务控制白名单
