# 更新摘要 / Changelog

## v1.1.3

**修复**
- 修复切换到「远程」tab 时先闪一帧“还没有远程电脑”空态、再刷新出列表的体验问题：页面数据改为构造时同步预载，首帧即显示已保存链接
- 「管理」tab 的探针主机与 SSH 列表同样做了首帧预载，消除同类闪烁

## v1.1.2

**优化**
- 「安全」开关指示器移至与标题同一行，版式更协调
- 「检测更新」按钮移至标题位并移除重复标题，卡片更简洁
- **备份与恢复纳入远程链接**：导出/恢复均包含「远程」页添加的 ZCode 链接（同 id 覆盖、其余保留），恢复完成提示分别列出三类数量

## v1.1.1

**修复/优化**
- 开启「生物识别锁」前需**先通过指纹/PIN 验证**才生效：设备未录入凭据或验证未通过/取消时保持关闭，防止误开或被他人开启
- 设置页各分区卡片**统一宽度**；「状态刷新间隔」选项自动换行，60秒 选项不再被裁切

## v1.1.0

**新功能**
- 集成 **Zrmt（ZCode 远程控制链接管理器）**：新增底部导航「**远程**」tab
  - 卡片式管理多台电脑的 ZCode 远程控制链接（粘贴链接自动解析电脑名称与版本号，需含 sid/hash 参数）
  - 点击卡片在 App 内 WebView 打开操作界面：支持旋转、桌面/手机版 UA 切换、错误重试、网页内附件/图片上传（系统文件选择器接入）、摄像头/麦克风网页授权
  - 支持从其他应用「分享链接」到云枢快速添加
- 首页改版：底部新增 **管理 / 远程 / 设置** 三 tab 导航栏；右下角悬浮「＋」添加菜单移至**右上角**

**优化**
- 整体代码清理：移除非必要的调试/测试辅助代码与死代码（未使用函数、Token 随机生成按钮等）

## v1.0.6

**修复（国产 ROM 专项二轮）**
- ColorOS 15 上 5 条 SAF 通道全部同步失败，说明问题出在公共启动路径：新增**第 6 通道「传统 startActivityForResult」**，完全绕开 androidx ActivityResultRegistry 与 ActivityOptions，直接走框架最老路径
- 全通道失败时改为**弹窗展示每条通道的完整异常**并支持一键复制（此前仅 toast），同时写入诊断日志，便于精确定位 ROM 拦截点
- 粘贴私钥输入始终可用（效果与选文件完全一致）

**提示**
- 若第 6 通道也失败且详情指向权限/管控，请在 ColorOS「设置 → 应用 → 云枢」中检查权限与电池策略；或直接使用粘贴私钥

## v1.0.5

**修复（国产 ROM 专项）**
- 定位到选私钥闪退/被切出前台的根本原因：**ColorOS 15 等系统对 ACTION_GET_CONTENT 通配 MIME 会同步抛出 IllegalArgumentException**（感谢用户提供的诊断信息确认）
- 文件选择改为 **5 通道自动回退**，任一通道可用即可打开：OpenDocument → GetContent(二进制) → GetContent(文本) → GetContent(通配) → 系统选择器 Chooser 中转
- 所有通道失败时明确提示改用「粘贴私钥内容」，并将每条通道的详细异常**写入诊断日志**（设置 → 关于 → 复制诊断日志），便于继续定位
- 粘贴私钥输入（自动识别 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK）保持可用，作为终极保底

## v1.0.4

**修复**
- 彻底解决「添加 SSH 主机选择本地私钥时 App 被切出前台」：
  - 新建表单**草稿持久化**（Keystore 加密落盘），即使部分 ROM 在系统文件选择器置顶期间查杀后台进程，返回应用后表单与所选私钥**原样恢复**，不再前功尽弃
  - 私钥支持**直接粘贴输入**（自动识别 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK 格式），完全不依赖系统文件选择器
  - 文件选择器启动失败（精简 ROM 无组件等）时给出明确提示并引导粘贴输入，不再静默退出

**说明**
- 文件选择基于系统 SAF 机制，**无需任何存储权限**（应用仅申请网络/生物识别/安装更新三项权限）
- 若你的 ROM 会在选择器打开时查杀应用，建议在系统设置中将云枢的电池策略设为「无限制/不限制后台」

**新增**
- 设置 → 关于：**复制诊断日志**（设备信息 + 最近一次崩溃堆栈）到剪贴板，便于反馈问题

## v1.0.3

**修复**
- 修复开启指纹锁后，添加 SSH 主机选择本地私钥文件时弹出认证、表现为被切出前台的问题（此前每次回到前台都会重新触发认证）

**变更**
- 生物识别认证策略调整为**仅在应用启动时验证一次**：切换后台、使用系统文件选择器、修改配置等场景均不再触发认证；锁定页支持「重试解锁」

## v1.0.2

**变更**
- 应用更名为「**云枢**」（英文 **Remoto**），应用包名变更为 `com.sundys.remoto`（与旧版本并存安装，需卸载旧版）

**体验优化**
- 首页新建按钮改为**圆形「＋」**悬浮按钮，SSH 列表**上滑自动隐藏、下滑恢复显示**
- 修复：开启生物识别锁后，添加 SSH 主机选择本地私钥时会被误锁切出前台的问题（系统文件选择器期间不再触发应用锁）

**项目**
- 探针 Release 资产改为固定名称（serverprobe-linux-amd64/arm64.tar.gz），README 提供自动识别架构的**探针一键安装命令**（含国内加速前缀说明）

## v1.0.1

**新功能**
- 设置页新增「检测更新」：一键检测 GitHub 最新 Release，支持**多通道检测与下载**（直连 + gh-proxy / ghfast / moeyy / ghproxy 等加速代理自动回退），解决部分网络无法访问 GitHub 的问题
- 检测到新版本直接弹出更新窗口，展示更新摘要与安装包大小，一键「下载并安装」
- 下载过程实时显示**进度条与已下载/总大小/百分比**，下载完成后自动唤起系统安装器
- 新增「关于」菜单：应用介绍，页面底部展示开源主页入口（GitHub 图标 + 「Github主页」超链接直达仓库）

**优化**
- 正式包按 ABI 拆分（armv7 / arm64）并使用固定签名，更新包可直接覆盖安装

## v1.0.0

🎉 首个公开版本：服务探针管理系统（Android 管理端 + Go 服务器探针）

**Android 管理端**
- 首页大卡片左右滑动切换主机，实时显示 CPU / 内存 / 磁盘 / 网速 / 负载 / 运行时长与 CPU 走势图
- 主机详情页：每核 CPU、内存与交换、多磁盘、网卡速率、systemd 服务列表（支持启动/停止/重启），底部 SSH 快速连接菜单
- SSH 终端：密码 + 私钥认证，私钥兼容 OpenSSH / PEM PKCS#1 / PKCS#8 / PuTTY PPK v2/v3；自研 VT100 256 色终端，控制键条、粘贴、字号调节；首次连接指纹确认（TOFU 防中间人）
- 添加探针向导：内置 SSH 设置项（绑定已有身份或新建）、连接测试、证书指纹获取与锁定
- 设置：日间/夜间/跟随系统显示模式、刷新间隔、生物识别锁、口令加密备份与恢复（PBKDF2 + AES-256-GCM）
- 凭据全部经 Android Keystore AES-256-GCM 加密存储；全局禁止明文流量

**Go 服务器探针**（本仓库 `probe/` 目录，linux amd64 / arm64）
- 零第三方依赖单二进制，systemd 常驻，一键安装脚本
- CPU/内存/磁盘/网络/负载/进程/服务状态采集 API
- Bearer Token 常量时间校验 + 每 IP 限流 + TLS 自签证书 + 服务控制白名单
